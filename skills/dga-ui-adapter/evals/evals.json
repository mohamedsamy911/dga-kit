{
  "skill_name": "dga-ui-adapter",
  "$generated": "DO NOT EDIT - generated by https://github.com/mohamedsamy911/dga-kit/blob/master/evals/build-evals-json.py from https://github.com/mohamedsamy911/dga-kit/blob/master/evals/dga-ui-adapter/cases/",
  "evals": [
    {
      "id": 1,
      "name": "mui-text-secondary-collision",
      "prompt": "Use the dga-ui-adapter skill.\n\nA team wires the DGA tokens into a MUI theme for a `.gov.sa` portal:\n\n```ts\npalette: {\n  text: { primary: t.role.text.default, secondary: t.role.text.secondary },\n  background: { default: t.role.background.body, paper: t.role.background.card },\n}\n```\n\nThey ask whether the theme is DGA-compliant.",
      "expected_output": "Both sides of the mapping are named `text.secondary`. This is the single highest-value finding\nin the skill and the one a reviewer is most likely to skim past.",
      "expectations": [
        "finds: `t.role.text.secondary` is DGA's gold **#dba102** and measures **2.30:1 on white** — below the 4.5:1 small-text threshold *and* the 3:1 large-text one",
        "names the mechanism: **MUI's `text.secondary` is a muted grey; DGA's is gold.** Mapping name-to-name looks obviously correct and ships an AA failure on every muted caption in the app",
        "fix names: `t.role.text['secondary-paragraph']` (#6c727e)",
        "points at `check-contrast.mjs` as the enforcement, not a code review",
        "must NOT: accept it because the token is genuinely DGA's",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 2,
      "name": "shadcn-muted-foreground",
      "prompt": "Use the dga-ui-adapter skill.\n\nA shadcn/ui project's `globals.css`:\n\n```css\n:root {\n  --background: var(--dga-background-body);\n  --foreground: var(--dga-text-default);\n  --muted-foreground: var(--dga-text-secondary);\n  --radius: var(--dga-radius-md);\n}\n```",
      "expected_output": "The same rule through CSS variables rather than a JS theme, paired with a correct line to see\nwhether the model flags indiscriminately.",
      "expectations": [
        "finds: `--muted-foreground: var(--dga-text-secondary)` — the same 2.30:1 failure as case 01, and `--muted-foreground` drives helper text, placeholders and timestamps across every shadcn component, so one line breaks contrast app-wide",
        "fix: `var(--dga-text-secondary-paragraph)`",
        "must NOT flag `--radius: var(--dga-radius-md)` as wrong — it is correct",
        "may note that DGA's radius scale is **not monotonic** (`2xl` and `3xl` are smaller than `xl`), as a Note, not a finding",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 3,
      "name": "wrong-library-advice",
      "prompt": "Use the dga-ui-adapter skill.\n\n\"We're building the government portal in **Vue 3 with PrimeVue**. How do we make the tab bar\nDGA-compliant?\"",
      "expected_output": "The skill names four libraries. A fifth must degrade to \"here is the DGA rule, map it yourself\",\nnot to confident invention of an API. Getting the *rule* right while admitting the API is unknown\nis a full pass.",
      "expectations": [
        "answers for the library actually in use, or states plainly that PrimeVue is not one of the four named in `component-mapping.md` and gives the **DGA rule** so it can be mapped onto whatever PrimeVue calls the component",
        "the DGA rule: a horizontal tablist **never scrolls and never wraps** — overflow goes to a \"More\" button. Most libraries default to scrolling",
        "also: six states including Focused, 44px targets, RTL mirroring",
        "FAIL (library confusion) if it answers with `Tabs` props from MUI, Chakra or Ant as though they were PrimeVue's, or invents a PrimeVue API",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 4,
      "name": "arabic-letter-spacing",
      "prompt": "Use the dga-ui-adapter skill.\n\nA Tailwind config extends the DGA display scale and a developer applies it to a bilingual page:\n\n```html\n<h1 class=\"text-display-lg tracking-tight\">الخدمات الرقمية</h1>\n```",
      "expected_output": "Nothing errors, nothing fails a build, and it looks correct to a reviewer who does not read\nArabic. Severity is as much the test here as detection.",
      "expectations": [
        "finds: DGA's display scale already carries **-0.02em tracking** (design spec says −2%; CSS `letter-spacing` does not accept percentages), and `tracking-tight` compounds it. Arabic is a **connected script** — letter-spacing breaks the joins and renders the word as detached glyphs",
        "fix: scope tracking to Latin, or zero it under `[dir=\"rtl\"]`",
        "must NOT: describe this as a stylistic preference or a \"may look better\" note. It is a DGA rule, and it makes Arabic text wrong rather than ugly",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 5,
      "name": "content-switcher-cap",
      "prompt": "Use the dga-ui-adapter skill.\n\nA dashboard uses a segmented control with six options: All, Pending, Approved, Rejected,\nExpired, Archived. The library renders it fine at desktop width.",
      "expected_output": "It renders correctly and reads fine. There is no visual symptom, so this is only ever caught by\nknowing the number.",
      "expectations": [
        "finds: DGA caps the Content Switcher at **2-4 options**. Six exceeds it",
        "fix: DGA's own remedy — use **Tabs** beyond four options",
        "cites the component spec rather than a general usability argument",
        "must NOT: soften it to \"consider using tabs\". The cap is a stated DGA limit",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 6,
      "name": "datepicker-assumed-hijri",
      "prompt": "Use the dga-ui-adapter skill.\n\n\"Our UI library ships a DatePicker with locale support and we've set `locale: 'ar-SA'`. That\ncovers the Hijri requirement, right?\"",
      "expected_output": "Two failure modes in one case. Fabricating a DGA Hijri requirement is the obvious one; the subtler\none is accepting `ar-SA` as sufficient, which is a plausible and wrong technical claim.",
      "expectations": [
        "corrects the premise: `ar-SA` changes locale formatting, **not the calendar system**. No mainstream UI library ships a Hijri calendar, and **DGA's own package contains no Hijri code** — its datepicker demo is Gregorian",
        "states DGA is **silent** on the calendar question: it publishes no Hijri guidance at all, so whether to show Hijri is the **project's** decision, recorded in `dga-brand-overlay`",
        "if the project has chosen Hijri: points at `dga-react/assets/reference-impl/dga-date.ts` (Umm al-Qura, tested, framework-free) and says the wrapper must be built",
        "must NOT: claim DGA *requires* Hijri",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 7,
      "name": "hardcoded-hex-in-component",
      "prompt": "Use the dga-ui-adapter skill.\n\nThe project has a fully wired DGA theme. A new component ships with:\n\n```tsx\n<div style={{ backgroundColor: '#f9fafb', borderRadius: 8 }}>\n```\n\nThe developer points out that `#f9fafb` **is** the correct DGA value for `background.body`.",
      "expected_output": "The developer is factually right about the value. The rule is about the reference, not the colour,\nand a model that only diffs hex values against `tokens.json` will pass this through.",
      "expectations": [
        "still a finding: the value is right and the **reference** is wrong. A literal hex cannot follow the theme, so it survives a dark-theme rollout, a re-harvest and a brand overlay by silently going stale",
        "notes DGA documents dark values for every semantic role, so a hardcoded light value is a guaranteed dark-mode bug the day that lands",
        "fix: reference the semantic role, and the same for `borderRadius: 8` -> the radius token",
        "must NOT: accept \"but it's the correct value\" as closing the issue",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 8,
      "name": "footer-missing-a11y-tools",
      "prompt": "Use the dga-ui-adapter skill.\n\nA team's footer component is complete: entity logo, sitemap links, social icons, contact block,\ncopyright, and the platform's last-modified date. Their axe-core run is clean and their Lighthouse\naccessibility score is 100. They ask whether the footer is done.",
      "expected_output": "Green tooling as false reassurance. Tests whether the model understands *why* the DGA-only\nrequirements need a human or a checklist, rather than just reciting the list.",
      "expectations": [
        "finds: the footer is **missing Accessibility Tools** — DGA lists font-size and contrast controls as required footer anatomy, and they must come **first in tab order**",
        "names why the tooling missed it: this is a **DGA requirement, not a WCAG one**, so no automated accessibility tool will ever flag its absence. A clean axe run is not evidence here",
        "also finds: only one last-modified date. DGA requires **two** — page *and* platform",
        "may note that the feedback section belongs on every page",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 9,
      "name": "silent-motion",
      "prompt": "Use the dga-ui-adapter skill.\n\n\"What duration and easing does DGA specify for the slide-out menu transition? We want to match\nthe spec exactly.\"",
      "expected_output": "The question presupposes a spec exists and asks to \"match it exactly\". Inventing \"200ms\nease-in-out\" is both plausible and completely unsourced.",
      "expectations": [
        "states plainly that **DGA publishes no motion tokens** — no durations, no easings, anywhere in Platforms Code",
        "names the fallback as the **project's** choice, recorded in `dga-brand-overlay`, not DGA's",
        "does surface what DGA *does* say about motion: `prefers-reduced-motion` is required in three places (Loading, Skeleton, Filtration)",
        "FABRICATION if it produces a duration in ms or an easing curve attributed to DGA",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 10,
      "name": "silent-card-aria",
      "prompt": "Use the dga-ui-adapter skill.\n\n\"Give me the ARIA roles DGA specifies for the Card component. We're building a selectable card\ngrid.\"",
      "expected_output": "There is text on DGA's Card page under an accessibility heading. Quoting it is the trap: it is\nreal published text that says the wrong thing about the wrong component.",
      "expectations": [
        "states that **DGA publishes no Card accessibility guidance** — the accessibility section on DGA's Card page is **Accordion's, pasted in**. The page is defective at source",
        "does not pass Accordion's roles off as Card's",
        "falls back to WCAG 2.1 AA and the appropriate ARIA pattern for a selectable grid, **labelled as the fallback**, not as DGA's requirement",
        "FABRICATION if it presents any role set as DGA-specified for Card",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 11,
      "name": "silent-dark-value",
      "prompt": "Use the dga-ui-adapter skill.\n\n\"We're implementing dark mode. What's DGA's dark value for `text.default`? The light one is\n#161616.\"",
      "expected_output": "Two, in opposite directions.\n\n**The old one, now inverted.** `#ffffff` is one token away and looks like a guess — and it is\nalso the real published value. An answer cannot be graded on whether it produced a hex.\n\n**The new one, and the reason this case was rewritten.** This case used to require the answer\n\"DGA publishes dark values only in Figma.\" That was true when written and is false now: the\nharvest found 402 dark declarations in the public CSS. A refusal to give the value is now the\nFALSE NEGATIVE — withholding published evidence is the same failure as inventing it. Cite-or-omit\ncuts both ways.\n\nThe answer that passes is neither the hex alone nor the refusal: it is the value, its provenance,\nand the reason it cannot be switched on.",
      "expectations": [
        "gives the value — **`#ffffff`** — and cites it to `role.dark.text.default` in `tokens.json`, read from the `[data-theme=dark] :root` rule in DGA's public CSS on 2026-08-27",
        "states in the same breath that it is **audit-only and unactivatable upstream**: the selector can never match, because `:root` is `<html>` and a descendant combinator needs an ancestor it does not have. Verified live: zero elements matched",
        "warns that the set as a whole is **not safe to ship as a dark theme** — five `*-light` status surfaces have no dark tint at all, so white text on them measures **1.05:1**",
        "says the values are deliberately **not** generated into `tokens.css`, and why: correcting the selector would activate those pairings for anyone already using `data-theme=\"dark\"`",
        "build the semantic layer with the dark slot **present**, filled from a project decision rather than from these values wholesale",
        "FABRICATION if it emits a hex DGA does not publish, or attributes an *activated* DGA dark theme to the source",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    },
    {
      "id": 12,
      "name": "clean-theme-wiring",
      "prompt": "Use the dga-ui-adapter skill.\n\nA Chakra v3 theme for a `.gov.sa` platform, submitted for review.\n\n**Colour roles.** Semantic roles from `tokens.json` under `semanticTokens.colors`. `text.secondary`\nis **kept as a defined role but repointed** from DGA's gold `#dba102` (2.30:1, unusable) to\n`secondary-gold.800` `#945c01` — 5.54:1 on white, 5.30:1 on body. The three `-light` roles are\n**kept as published** and documented as dark-surface only: on `background.black` they measure\n10.75:1, 14.79:1 and 9.09:1. Nothing is deleted from the theme, so no token can resolve to a raw\nstring.\n\n**Brand palette.** Chakra's seven `colorPalette` slots are filled and every composed pairing was\nmeasured: `subtle` brand.50, `muted` brand.100, `emphasized` brand.200, `solid` brand.600 with\n`contrast` white at **4.75:1**, `focusRing` brand.600, and **`fg` brand.700 `#166a45`** — 6.31:1 on subtle,\n5.81:1 on muted, 4.92:1 on emphasized, 6.60:1 on white. Solid-button hover and pressed resolve to\nbrand.700 and brand.800 (6.60:1 and 8.56:1 against white); brand.500 is explicitly excluded as a\nfill because white on it is 3.88:1.\n\n**Dark mode.** No `_dark` slot is emitted and no colour-mode toggle is exposed. DGA publishes dark\nvalues only in the PC 1.0 Figma collections, so there is nothing legitimate to put in a slot; an\nempty-but-present slot filled with the light value would ship a light theme labelled dark. The\ngap is recorded in `dga-brand-overlay`.\n\n**Type.** `heading` and `body` are `\"IBM Plex Sans\", \"IBM Plex Sans Arabic\", system-ui,\nsans-serif` — **Latin first**, so Latin renders in the face DGA actually names and Arabic\nfalls through per character to the Arabic face. The Arabic face is recorded in\n`dga-brand-overlay` as the project's decision pending DS-DGA@dga.gov.sa — not cited as a DGA\nrule. Saudi Font is exposed only as `fonts.occasion`.\nDisplay tracking is emitted as `-0.02em` (never `-2%`, which is invalid CSS) and scoped\n`:lang(en)`, so an unlabelled document gets **no** tracking rather than tracking on Arabic.\n\n**Breakpoints.** `sm: 600px, md: 960px, lg: 1280px`, with Chakra's inherited `xl`/`2xl` deleted so\nan unknown key cannot silently no-op. Documented as **DGA's four bands under three Chakra names** —\nmobile 0–599, tablet 600–959, desktop 960–1279, xl 1280+ — with a comment noting that DGA\n\"desktop\" is Chakra `md`.\n\n**Spacing.** Wired in rem rather than DGA's published px, recorded as a deliberate decision: DGA\nmandates a footer font-size control, and px spacing would scale the text without scaling the boxes\naround it. Computed values match DGA's scale at the default root size.\n\n**Controls.** 44px minimum applied per element type, not blanket: directly on `IconButton`,\n`CloseButton`, `Menu.Item` and `Tabs.Trigger`; and on `Checkbox.Root`, `RadioGroup.Item`,\n`Switch.Root` and `Tag.Root` via padding and an inset `::before` hit area — so the *target* is\n44px without the painted control box, switch track or close glyph being resized. `Link` sets\n`textDecoration: 'underline'` in its base.\n\n**CI.** `check-contrast.mjs --json` runs as a committed artefact documenting which upstream DGA\ntokens are unusable. The **blocking** checks are separate and run over the project's own source: a\ngrep for hex literals outside the theme layer, a grep for `#dba102` / `background.secondary` /\n`colorPalette=\"secondary\"` (the aliases a developer would actually reach for), and a contrast\nassertion over the composed brand pairings listed above.",
      "expected_output": "The rubric tests what restraint actually means for a reviewer: no invented rules, no inflated\nseverity, no Blockers where none exist, and credit where the submission is right.\n\nTwo things a padding reviewer will do here and a calibrated one will not: grade the documented\nFigma-only gaps as findings rather than Notes, and treat the build list as defects in a theme\nthat structurally cannot contain them.\n\nThe Arabic-first font stack is the sharpest trap. It *looks* like the Arabic-first principle\napplied to typography and it is not: font fallback resolves **per character**, so an\nArabic-first stack renders Latin out of IBM Plex Sans Arabic's Latin glyphs and the face DGA\nnames never renders anything. A reviewer flagging that order is **correct**.",
      "expectations": [
        "**Zero Blockers.** Nothing here is mandated-and-absent.",
        "**Leads with the verdict and with what is correct**, before any findings.",
        "**Every finding must be verifiable** — a contrast ratio that reproduces against `tokens.json`, a documented Chakra or DGA behaviour, or a named omission in the submission itself. A finding that rests on a DGA rule which does not exist is a fabrication and fails the run outright.",
        "**Severity calibrated.** Nothing mandated-and-absent graded below High; nothing stylistic graded above Low. A response that is uniformly severe fails.",
        "**Notes, not findings**, on limits documented as unavailable upstream: responsive radius and spacing being Figma-only; DGA's non-monotonic radius scale.",
        "The **build list** — Digital Stamp, footer Accessibility Tools, feedback section, skip link, table of contents, two last-modified dates, Hijri wrapper — is **scope, not a finding**. **FAIL if:** a Blocker is raised · any finding cites a DGA rule that does not exist · the response is uniformly severe · it does not say what the submission got right.",
        "Cites DGA for every rule it asserts, and does not invent one: a rule attributed to DGA that DGA does not publish fails this case regardless of what else the answer gets right."
      ]
    }
  ]
}
