interface:
  display_name: "DGA Accessibility Audit"
  short_description: "Audit against WCAG 2.1 AA and DGA rules"
  brand_color: "#1B8354"
  default_prompt: "Use $dga-a11y to audit this interface for accessibility issues."

policy:
  # Codex defaults this to true. Pinned rather than inherited: these are compliance
  # skills, and the failure mode of a changed default is that Codex stops invoking them
  # AUTOMATICALLY. The skill would still load - it just never fires unless someone types
  # $dga-a11y by hand, and a team then ships believing it was checked.
  allow_implicit_invocation: true
