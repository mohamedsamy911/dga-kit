interface:
  display_name: "DGA Mockup"
  short_description: "Build a compliant screen or wireframe"
  brand_color: "#1B8354"
  default_prompt: "Use $dga-mockup to produce a DGA-compliant mockup of this screen."

policy:
  # Codex defaults this to true. Pinned rather than inherited: these are compliance
  # skills, and the failure mode of a changed default is that Codex stops invoking them
  # AUTOMATICALLY. The skill would still load - it just never fires unless someone types
  # $dga-mockup by hand, and a team then ships believing it was checked.
  allow_implicit_invocation: true
