interface:
  display_name: "DGA Developer Handoff"
  short_description: "Turn a design into a DGA handoff spec"
  brand_color: "#1B8354"
  default_prompt: "Use $dga-handoff to turn this approved design into a developer spec."

policy:
  # Codex defaults this to true. Pinned rather than inherited: these are compliance
  # skills, and the failure mode of a changed default is that Codex stops invoking them
  # AUTOMATICALLY. The skill would still load - it just never fires unless someone types
  # $dga-handoff by hand, and a team then ships believing it was checked.
  allow_implicit_invocation: true
